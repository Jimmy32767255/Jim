# 事件触发器

当程序开始运行时:
> 程序入口点，所有初始化代码应放在此触发器下

当程序发生未捕获的异常时:

当鼠标移动:

当鼠标点击:

当键盘按下:

当用户在终端输入任意内容时:

当接收到^C中断信号时:

当系统内存不足时:

当收到网络数据时:

当网络连接断开时:

当定时器()触发时:
> 周期性触发的事件
> 参数：(整数) 定时秒数
> 选项：无

当每天()时:
> 每日定时触发
> 参数：(字符串) 时间格式"HH:MM"
> 选项：无

当每周()时: